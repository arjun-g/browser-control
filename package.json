{
  "name": "browser-control",
  "version": "0.1.0",
  "private": true,
  "description": "MCP + browser extension bridge for controlling Chrome/Edge sessions",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build -w @browser-control/shared && npm run build -w @browser-control/mcp-server",
    "dev": "npm run dev -w @browser-control/mcp-server",
    "typecheck": "npm run typecheck -w @browser-control/shared && npm run typecheck -w @browser-control/mcp-server",
    "test": "npm run test -w @browser-control/mcp-server"
  },
  "engines": {
    "node": ">=20"
  }
}
